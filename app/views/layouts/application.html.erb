<!DOCTYPE html>
<html>
  <head>
    <title class="memo-overheader"><%= content_for(:title) || "Memolink" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <header class="bg-gray-100 p-4 shadow">
  <nav class="max-w-4xl mx-auto flex justify-between items-center">
    <div class="text-xl font-bold">
      <%= link_to "Memolink", root_path %>
    </div>

    <div class="flex gap-4 items-center">
      <% if user_signed_in? %>
        <span>こんにちは、<%= current_user.email %> さん</span>
        <%= link_to "テンプレート入力", new_template_path, class: "bg-green-500 text-white px-3 py-1 rounded hover:bg-green-600" %>
        <%= link_to "一覧を見る", templates_path, class: "bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600" %>
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete, data: { turbo_confirm: "ログアウトしますか？" }, class: "bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600" %>
      <% else %>
        <%= link_to "ログイン", new_user_session_path, class: "px-3 py-1 hover:underline" %> |
        <%= link_to "新規登録", new_user_registration_path, class: "px-3 py-1 hover:underline" %>
      <% end %>
    </div>
  </nav>
</header>



   <!-- flash メッセージ -->
<div class="container mx-auto px-6 mt-4">
  <% flash.each do |key, message| %>
    <% color_class = case key.to_sym
      when :notice then "bg-green-100 border-green-400 text-green-700"
      when :alert then "bg-red-100 border-red-400 text-red-700"
      else "bg-gray-100 border-gray-400 text-gray-700"
    end %>

    <div class="border-l-4 p-4 mb-4 <%= color_class %> rounded shadow">
      <p><%= message %></p>
    </div>
  <% end %>
</div>


    <main>
      <%= yield %>
    </main>
  </body>
</html>

<style>
  .memo-overheader {
    color: #e15b64;
    float: left;
    font-size: 60px;
    text-decoration: none;
  }

</style>